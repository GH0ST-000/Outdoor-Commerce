import type { ProductDetailFixture } from "@/features/storefront/types/storefront-types";

/** Future public catalog API will satisfy this shape. */
export type CatalogProductView = ProductDetailFixture;
